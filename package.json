{
  "private": true,
  "scripts": {
    "postinstall": "bower install",
    "build": "pulp build && rimraf docs && pulp docs",
    "test": "cd check-laws && bower install && pulp test"
  },
  "devDependencies": {
    "pulp": "^6.1.0",
    "bower": "^1.7.2",
    "rimraf": "^2.4.1"
  }
}
